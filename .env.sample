# Optional: Your name (used by TTS notifications if enabled)
ENGINEER_NAME=

# Optional: Enable AI-generated event summaries (add --summarize to hooks)
# ANTHROPIC_API_KEY=

# Optional: Enable TTS notifications (add --notify to hooks)
# OPENAI_API_KEY=
# ELEVENLABS_API_KEY=

# MongoDB (auto-started via Docker Compose)
# MONGODB_URI=mongodb://localhost:47217/observability
# MONGODB_PORT=47217
